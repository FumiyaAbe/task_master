<div class="mx-auto max-w-3xl px-4 py-6">
  <div class="page-head mb-4">
    <h1 class="text-2xl font-bold">タスクを編集</h1>
    <div class="flex gap-2">
      <%= link_to "詳細へ戻る", task_path(@task), class: "btn-ghost" %>
    </div>
  </div>

  <%= render "form", task: @task %>
</div>
