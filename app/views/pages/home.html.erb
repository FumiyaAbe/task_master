<div class="mx-auto max-w-3xl p-4">
  <h1 class="text-2xl font-bold mb-4">Task Master</h1>

  <% if user_signed_in? %>
    <p class="mb-4">ログイン中：<%= current_user.email %></p>
    <div class="flex gap-3">
      <%= link_to "Tasks",  tasks_path,  class: "btn-main" %>
      <%= link_to "Events", events_path, class: "btn-accent" %>
      <%= button_to "ログアウト",
            destroy_user_session_path,
            method: :delete,
            class: "btn-ghost" %>
    </div>
  <% else %>
    <p class="mb-4">アカウントを作成して、タスクと予定を管理しましょう。</p>
    <div class="flex gap-3">
      <%= link_to "ログイン",   new_user_session_path,      class: "btn-main" %>
      <%= link_to "新規登録",   new_user_registration_path, class: "btn-accent" %>
    </div>
  <% end %>
</div>
